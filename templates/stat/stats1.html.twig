{% extends 'front.html.twig' %}
{% block stat %}

    <div class="container">
        <h1> &nbsp &nbsp &nbsp &nbsp &nbsp &nbsp     Pourcentage par Listes Favoris  <a href="{{ path('app_favoris_index') }}">  
        Afficher la liste des favoris</a></h1>
        <canvas id="matchs" width="2000" height="500"></canvas>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js" integrity="sha256-t9UJPrESBeG2ojKTIcFLPGF7nHi2vEc7f5A2KpH/UBU=" crossorigin="anonymous"></script>
    <script>
        let liv = document.querySelector("#matchs")
        let matchGraph = new Chart(liv, {
            type: "pie",
            data: {
                labels:  ['bien','Excelent'],
                datasets: [{
                    label: "Répartition des catégories",
                    data: ['{{ liv }}', {{ cl }}],
                    backgroundColor: ['#FF5733','#FFBD33']
                }]
            }
        })
    </script>
    </div>                </div>

{% endblock %}